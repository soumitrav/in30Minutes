1. remove babel and webpack related packages from package.json
2. Start script as follows 
"start": "set PORT=4200 && react-scripts start",
3.np install installs the packages to node_modules folder and should be deleted to free up the space.
4.
docker build . to create the front end docker image
docker front end image creation is in multi staging process.

first it will copy the the files to nodejs docker and create a build and after that it will copy the build to ngnix docker 
image adn run it.

.dockerignore to ignore the folder

tag a build 

docker build . -t soumitrav/app-front-end:0.0.1

